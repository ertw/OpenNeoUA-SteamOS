cmake_minimum_required(VERSION 3.10)

if(POLICY CMP0095)
    cmake_policy(SET CMP0095 NEW)
endif()

project(OpenNeoUA)

list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")

set(CMAKE_CXX_STANDARD 14)

set(SOURCES
    3ds.cpp
    ade.cpp
    area.cpp
    amesh.cpp
    bitmap.cpp
    base.cpp
    bmpAnm.cpp
    button.cpp
    crashdiag.cpp
    crc32.cpp
    def_parser.cpp
    embed.cpp
    env.cpp
    fmtlib/format.cc
    font.cpp
    gui/root.cpp
    gui/uabutton.cpp
    gui/uacommon.cpp
    gui/uaempty.cpp
    gui/uamsgbox.cpp
    gui/uascroll.cpp
    gui/uawindow.cpp
    gui/widget.cpp
    idev.cpp
    ilbm.cpp
    image.cpp
    ini.cpp
    itimer.cpp
    iwimp.cpp
    log.cpp
    loaders.cpp
    locale/default.cpp
    locale/locale.cpp
    lstvw.cpp
    main.cpp
    IFFile.cpp
    network.cpp
    netlib/client.cpp
    netlib/client_threads.cpp
    netlib/server.cpp
    netlib/server_session.cpp
    netlib/server_threads.cpp
    netlib/single.cpp
    netlib/single_threads.cpp
    netlib/zndNet.cpp
    nucleas.cpp
    obj3d.cpp
    particle.cpp
    rsrc.cpp
    sample.cpp
    skeleton.cpp
    sklt.cpp
    system/fsmgr.cpp
    system/gfx.cpp
    system/glfuncs.cpp
    system/inivals.cpp
    system/inpt.cpp
    system/luascript.cpp
    system/movie.cpp
    system/sound.cpp
    system/stdshdr.cpp
    system/system.cpp
    system/tform.cpp
    utils.cpp
    wav.cpp
    windp.cpp
    winp.cpp
    wintimer.cpp
    world/analyzer.cpp
    world/blacksecttint.cpp
    world/energyfx.cpp
    world/grounddecal.cpp
    world/weapontracer.cpp
    world/luaevents.cpp
    world/parsers.cpp
    world/nparticle.cpp
    world/protos.cpp
    world/saveparsers.cpp
    world/tools.cpp
    wrapal.cpp
    yw.cpp
    yw_brif.cpp
    yw_funcs.cpp
    yw_func2.cpp
    yw_game.cpp
    yw_game_ui.cpp
    yw_net.cpp
    yw_save.cpp
    ypabact.cpp
    ypacar.cpp
    ypaflyer.cpp
    ypagun.cpp
    ypamissile.cpp
    yparobo.cpp
    ypatank.cpp
    ypaufo.cpp
)

find_package(PkgConfig REQUIRED)
find_package(Threads REQUIRED)

pkg_search_module(SDL2 REQUIRED sdl2)
link_directories(${SDL2_LIBRARY_DIRS})

pkg_search_module(SDL2_IMAGE REQUIRED SDL2_image)
link_directories(${SDL2_IMAGE_LIBRARY_DIRS})

pkg_search_module(SDL2_TTF REQUIRED SDL2_ttf)
link_directories(${SDL2_TTF_LIBRARY_DIRS})

pkg_search_module(SDL2_NET REQUIRED SDL2_net)
link_directories(${SDL2_NET_LIBRARY_DIRS})

pkg_search_module(VORBISFILE REQUIRED vorbisfile)
link_directories(${VORBISFILE_LIBRARY_DIRS})

# FFmpeg moderno.
# Non cerca le vecchie librerie avresample e postproc.
pkg_check_modules(
    FFMPEG
    REQUIRED
    IMPORTED_TARGET
    libavformat
    libavcodec
    libavutil
    libswscale
    libswresample
)

find_package(OpenGL REQUIRED)
find_package(OpenAL REQUIRED)
find_package(Lua REQUIRED)

if(WIN32)
    enable_language(RC)
    set(
        WIN32_RESOURCES
        "${CMAKE_CURRENT_SOURCE_DIR}/../svg/icon/icon.rc"
    )
endif()

if(WIN32)
    add_executable(
        OpenNeoUA
        WIN32
        ${SOURCES}
        ${WIN32_RESOURCES}
    )
else()
    add_executable(
        OpenNeoUA
        ${SOURCES}
    )
endif()

target_link_libraries(
    OpenNeoUA

    ${SDL2_LIBRARIES}
    ${SDL2_IMAGE_LIBRARIES}
    ${SDL2_TTF_LIBRARIES}
    ${SDL2_NET_LIBRARIES}
    ${OPENGL_LIBRARIES}
    ${VORBISFILE_LIBRARIES}
    ${OPENAL_LIBRARY}
    ${LUA_LIBRARIES}

    PkgConfig::FFMPEG
    Threads::Threads
)

if(WIN32)
    target_link_libraries(OpenNeoUA dbghelp)
endif()

install(TARGETS OpenNeoUA RUNTIME DESTINATION bin)

# Keep the private overlay libraries resolvable when the installed executable
# is launched directly or from an AppImage.
if(UNIX AND NOT APPLE)
    set_target_properties(
        OpenNeoUA
        PROPERTIES
        INSTALL_RPATH "$ORIGIN/../lib"
        INSTALL_RPATH_USE_LINK_PATH FALSE
    )
endif()

target_include_directories(
    OpenNeoUA
    PUBLIC

    ${PROJECT_SOURCE_DIR}
    ${SDL2_INCLUDE_DIRS}
    ${SDL2_IMAGE_INCLUDE_DIRS}
    ${SDL2_TTF_INCLUDE_DIRS}
    ${SDL2_NET_INCLUDE_DIRS}
    ${OpenGL_INCLUDE_DIR}
    ${VORBISFILE_INCLUDE_DIRS}
    ${OPENAL_INCLUDE_DIR}
    ${LUA_INCLUDE_DIR}
)

if(CMAKE_BUILD_TYPE MATCHES [dD][eE][bB][uU][gG])
    target_compile_options(
        OpenNeoUA
        PUBLIC
        "-g"
        "-O0"
    )

    if(NOT WIN32)
        target_compile_options(
            OpenNeoUA
            PUBLIC
            "-fsanitize=address"
        )

        target_link_libraries(
            OpenNeoUA
            "-fsanitize=address"
        )
    endif()
endif()

# Non-Debug configurations deliberately keep CMake's native optimization
# flags; RelWithDebInfo must retain -O2 instead of being overridden by -O0.

enable_testing()

add_executable(test_gamepad_input tests/test_gamepad_input.cpp)
target_include_directories(
    test_gamepad_input
    PRIVATE
    ${PROJECT_SOURCE_DIR}
    ${SDL2_INCLUDE_DIRS}
)
target_link_libraries(test_gamepad_input ${SDL2_LIBRARIES})
add_test(NAME gamepad_input COMMAND test_gamepad_input)
